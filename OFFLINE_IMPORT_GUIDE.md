# 离线脑电信号导入功能使用指南

## 功能概述

本系统新增了离线脑电信号导入功能，允许用户导入本地EEG数据文件进行离线分析和可视化。支持小波自适应滤波、ERD/ERS特征提取、SVM分类以及3D大脑源成像可视化。

## 主要特性

### 1. 文件格式支持
- **CSV格式**：完全支持，包含时间戳和通道数据
- **EDF格式**：计划支持（当前使用模拟数据）
- **MAT格式**：计划支持（当前使用模拟数据）
- **TXT格式**：计划支持（当前使用模拟数据）

### 2. 信号处理算法
- **小波自适应滤波**：使用db4小波进行多尺度分解和自适应阈值去噪
- **ERD/ERS特征提取**：计算事件相关去同步化/同步化特征
- **SVM分类**：简化的支持向量机进行运动想象分类
- **频谱分析**：μ节律(8-13Hz)和β节律(13-30Hz)功率分析

### 3. 可视化功能
- **3D大脑源成像**：基于Three.js的实时皮层活动可视化
- **EEG信号波形**：C3/C4通道滤波后信号显示
- **特征可视化**：ERD/ERS强度、相干性等特征图表
- **分类结果**：实时分类置信度和预测结果

## 使用步骤

### 1. 启动离线模式
1. 打开系统首页
2. 点击"导入离线脑电信号"按钮
3. 选择本地EEG数据文件
4. 系统自动跳转到离线分析模式

### 2. 数据格式要求

#### CSV文件格式
```csv
time,C3,C4
0.000,45.2,38.7
0.001,46.1,39.2
0.002,44.8,37.9
...
```

**要求：**
- 第一行必须包含列标题
- 必须包含`time`列（时间戳，单位：秒）
- 必须包含`C3`和`C4`通道数据
- 数据值应为微伏(μV)单位
- 建议采样率：1000Hz

### 3. 离线分析界面

#### 数据管理面板
- **导入EEG文件**：选择本地文件进行导入
- **生成模拟数据**：创建60秒的模拟EEG数据用于测试
- **加载历史数据**：加载之前保存的分析结果
- **保存当前数据**：保存当前分析结果

#### 播放控制面板
- **时间滑块**：控制分析时间点
- **前进/后退按钮**：快速跳转时间点
- **时间显示**：显示当前分析时间

#### 可视化面板
- **3D大脑可视化**：显示皮层活动热点
- **EEG信号图表**：显示滤波后的C3/C4通道信号
- **分类结果**：显示运动想象分类结果
- **特征分析**：显示ERD/ERS等特征值

## 技术实现

### 1. 文件解析器 (`src/lib/eegProcessor.ts`)
```typescript
// CSV文件解析
async parseCSVFile(file: File): Promise<EEGData>

// 小波自适应滤波
class WaveletAdaptiveFilter {
  filter(signal: number[]): number[]
}

// ERD/ERS特征提取
class ERDERSFeatureExtractor {
  extractFeatures(signal: number[], channel: string): FeatureData
}

// SVM分类器
class SimpleSVMClassifier {
  train(features: number[][], labels: number[]): void
  predict(features: number[]): number
}
```

### 2. 数据处理流程
1. **文件读取**：解析CSV文件，提取时间戳和通道数据
2. **小波滤波**：对每个通道进行自适应小波去噪
3. **特征提取**：计算ERD/ERS、频谱功率、相干性等特征
4. **分类预测**：使用SVM对特征进行分类
5. **可视化更新**：更新3D大脑和图表显示

### 3. 状态管理
- 使用React状态管理离线数据
- 通过sessionStorage持久化数据
- 支持实时时间点切换和可视化更新

## 示例数据

系统提供了示例CSV文件 `sample_eeg_data.csv`，包含：
- 100个时间点（0-0.1秒）
- C3和C4通道数据
- 模拟的运动想象相关信号

## 注意事项

1. **文件大小限制**：建议单个文件不超过100MB
2. **采样率要求**：建议使用1000Hz采样率以获得最佳效果
3. **数据质量**：确保数据包含足够的运动想象相关活动
4. **浏览器兼容性**：建议使用Chrome或Firefox最新版本

## 故障排除

### 常见问题
1. **文件导入失败**：检查文件格式是否正确
2. **处理速度慢**：大文件处理需要时间，请耐心等待
3. **可视化不显示**：检查数据是否包含C3/C4通道
4. **分类结果异常**：确保数据质量良好，包含明显的运动想象信号

### 调试信息
- 打开浏览器开发者工具查看控制台日志
- 检查网络请求和数据处理状态
- 验证导入数据的格式和内容

## 未来计划

1. **更多文件格式支持**：EDF、MAT、TXT等
2. **高级滤波算法**：ICA、CSP等
3. **更多特征提取**：时频分析、复杂度等
4. **批量处理**：支持多文件批量分析
5. **结果导出**：支持分析结果导出为多种格式
